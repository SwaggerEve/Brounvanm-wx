const initialMemberState = {
  isMember: false,
  selectedTier: ""
};

const joinedMemberState = {
  isMember: true,
  selectedTier: "basic"
};

module.exports = {
  initialMemberState,
  joinedMemberState
};
